<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codedata.rbac.system.dao.UserDao">

    <select id="getUserByName" resultType="com.codedata.rbac.system.entity.UserEntity">
        select * from sys_user where username = #{value}
    </select>

    <select id="getUserByUserId" resultType="com.codedata.rbac.system.entity.UserEntity">
        select * from sys_user where id = #{value}
    </select>

    <!-- 查询用户的所有权限 -->
    <select id="queryAllPerms" resultType="string">
        SELECT
            m.perms
        FROM
            sys_user_role ur
                LEFT JOIN sys_role_menu rm ON ur.rid = rm.rid
                LEFT JOIN sys_menu m ON rm.mid = m.id
        WHERE
            ur.uid = #{userId}
    </select>

    <!-- 查询用户的所有菜单ID -->
    <select id="queryAllMenuId" resultType="long">
        SELECT DISTINCT
            rm.mid
        FROM
            sys_user_role ur
                LEFT JOIN sys_role_menu rm ON ur.rid = rm.rid
        WHERE
            ur.uid = 1
    </select>


</mapper>